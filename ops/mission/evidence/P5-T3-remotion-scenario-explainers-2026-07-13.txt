P5-T3 GREEN EVIDENCE - REMOTION SCENARIO EXPLAINERS
Date: 2026-07-13
Branch: codex/mission/p5-remotion-scenario-explainers

DELIVERED CONTRACT
- Remotion 4.0.489, CLI 4.0.489, and bundler 4.0.489 are pinned only in
  apps/media with React 19.2.7, TypeScript 6.0.3, and exact Zod 4.3.6.
- Browser lifecycle scripts were disabled during installation.
- Web and Expo manifests remain renderer-free; the standing package-family and
  lockfile isolation contracts pass.
- Three pinned local WOFF2 fonts are copied into the ignored Remotion public
  directory before checks or renders. No remote runtime assets are used.
- Every CLI invocation goes through a wrapper that requires Chrome for Testing
  150.0.7871.115 exactly; renderer checks cannot auto-download a browser.
- CI installs that exact build through commit-pinned setup-chrome v2.1.2, while
  `npm run prepare:remotion-browser` is the explicit one-time local bootstrap.
- All motion is driven by Remotion frames and interpolate(). CSS animation,
  transitions, timers, wall-clock time, randomness, and remote URLs are barred
  by contract.
- A strict Zod discriminated union parses every committed scenario and each
  composition's props before rendering.

SHARED-ENGINE PROVENANCE
- apps/media/remotion/data/scenarios.v1.json is generated by importing directly
  from packages/financial-engine/src/index.ts.
- Engine contract version: source-sha256:995c9a4deec4
- Lender-terms contract version: 2.0.0
- Portable engine source SHA-256:
  995c9a4deec471201f19251ef555c5755e077e3296dea7d20fe26387f7b91413
- Stories:
  1. compatible-path baseline comparison: 49 modeled months / $3,143.65
     interest at $450 monthly versus 28 months / $1,753.89 at $750 monthly for
     the same labeled $18,500 and 7.9% APR assumptions;
  2. first Money Loop month: transaction-calendar LOC accrual with four exact
     dated closing-balance events, six ordered teaching steps, $750 cash flow,
     $14,171.79 debt, and $5,283.72 LOC ending balance;
  3. blocked plan: no payoff date when cash flow is -$250 and six lender terms
     are missing.
- `npm run generate:scenarios -- --check` passes and fails on any committed JSON
  drift from the shared engine.

FRESH STANDING GATE
Command:
  npm --prefix apps/media run check

Result: PASS
- 33/33 media contracts pass.
- HyperFrames: zero lint/runtime/motion errors or warnings, 40/40 contrast
  checks, and 26 mapped finite tweens. Three known informational layout probes
  remain non-blocking and unchanged from P5-T2.
- Remotion TypeScript passes.
- Remotion browser bundling passes.
- Ten registrations enumerate successfully: nine 8-second, 30-fps videos plus
  one reduced-motion still.
- Video matrix dimensions: 1920x1080, 1080x1920, and 1080x1080 for each of the
  baseline, Money Loop month, and blocked-plan stories.
- Browser runtime assertions fail rendering when the root scrolls, a marked item
  overflows, content escapes the exact safe area, or direct layout zones collide.
- Literal colors are restricted to the versioned identity palette covered by
  the standing contrast contract.
- The reduced-motion still preserves all six ordered Money Loop values and
  explanations, both ending balances, assumptions, and the disclaimer.

RENDER PROOF
Commands:
  npm --prefix apps/media run render:matrix
  npm --prefix apps/media run render:still
  npm --prefix apps/media run render:video

Results: PASS
- Twenty-seven PNGs rendered: intro frame 18, story frame 120, and resolve frame
  220 for all nine video compositions. Representative square and landscape
  phases were inspected at original resolution for clipping, overlap,
  typography, truth labels, safe areas, and hierarchy.
- The matrix renderer verifies the PNG signature and exact expected dimensions,
  then records byte size and SHA-256 for every artifact in its ignored manifest.
- Representative still: 1920x1080 PNG, 484,480 bytes,
  sha256 a3c0a060a6611114f1a71bb6a704ee4d05da74028627dc8b3c4e756656a505ab.
- Representative motion artifact: H.264, 1920x1080, 30/1 fps, 240 frames,
  8.042667 seconds, 984,179 bytes,
  sha256 2c5e5997a74ffe032452b6e93d0137f899e544819a491e63060d7d528593bf82.
- The full video evaluates the browser runtime geometry probes on every frame.
- The square overlap and frame-2 translated-safe-area failures found during red
  passes were fixed before the matrix and full video were regenerated.

PORTABILITY AND SUPPLY CHAIN
- HyperFrames source digests normalize CRLF to LF in both map generation and
  contract validation, so Windows and Ubuntu compare the same committed bytes.
- `npm --prefix apps/media audit --audit-level=high`: found 0 vulnerabilities.
- Generated fonts, browser bundles, render output, stills, videos, and matrix
  manifests remain ignored and cannot enter broad staging.

INDEPENDENT REVIEW
- The first review returned eight important and two minor findings; all were
  resolved and rechecked.
- A second review found two additional important gaps in reduced-motion parity
  and browser reproducibility; both were resolved and rechecked.
- Final verdict: no remaining findings. Spec Compliance: APPROVED. Code Quality:
  APPROVED.

VERDICT
P5-T3 is locally green. P5-G1 and P5-G2 have executable passing evidence, but
mission state remains in progress until PR checks, merge, and a fresh
current-main rerun are complete.
